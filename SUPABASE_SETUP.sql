-- JC Handyman Cloud CRM V12 Pro Polish

-- Fix signatures table for typed approvals
create table if not exists public.signatures (
  id bigint generated by default as identity primary key,
  created_at timestamptz not null default now(),
  customer_id bigint,
  signature_type text,
  title text,
  signature_url text
);

alter table public.signatures add column if not exists signature_type text;
alter table public.signatures enable row level security;

drop policy if exists "Allow public select signatures" on public.signatures;
drop policy if exists "Allow public insert signatures" on public.signatures;

create policy "Allow public select signatures"
on public.signatures
for select
using (true);

create policy "Allow public insert signatures"
on public.signatures
for insert
with check (true);
